name: self-hosted runner

on:
  workflow_dispatch:
  push:
    branches: [ ELN ]
# https://docs.knapsackpro.com/2021/how-to-load-ruby-gems-from-cache-on-github-actions

jobs:
  docker:
    runs-on: self-hosted
    steps:
      - name: Check file existence
        id: check_files
        uses: andstor/file-existence-action@v1
        with:
          files: "gitlab-runner/shared"
      - name: docker pull
        # todo check for newer tag
        if: steps.check_files.outputs.files_exists == 'false'
        run: |
          docker pull complat/complat-ubuntu-runner:0.10.20
          docker cp $(docker run -it -d complat/complat-ubuntu-runner:0.10.20):/home/gitlab-runner .
          # todo check if fully pulled
      # - name: Check shared files existence
      #   run: |
      #     echo "CHECK SHARED"
      #     cd gitlab-runner/shared
      #     ls

  checkout:
    needs: docker
    runs-on: self-hosted
    steps:
      - name: Check file existence
        id: check_files
        uses: andstor/file-existence-action@v1
        with:
          files: "chemotion_ELN/app"
      - name: init checkout ELN
      # todo always checkout?
        if: steps.check_files.outputs.files_exists == 'false' 
        uses: actions/checkout@v2
        with:
          repository: ComPlat/chemotion_ELN
          path: chemotion_ELN

  config_files:
    needs: checkout
    runs-on: self-hosted
    steps:
      - name: change config files
        run: |
            pwd
            ls
            cd chemotion_ELN/config
            cp database.yml.gitlab database.yml
            cp -f storage.yml.example storage.yml
            touch datacollectors.yml
            cp -f profile_default.yml.example profile_default.yml
            touch klasses.json

  db:
    needs: config_files
    runs-on: self-hosted
    services:
      # https://docs.github.com/en/actions/guides/creating-postgresql-service-containers#running-jobs-directly-on-the-runner-machine
      # Label used to access the service container
      postgres:
        image: postgres:12-alpine
        env:
          POSTGRES_DB: chemotion_test
          POSTGRES_USER: chemotion_test
          POSTGRES_PASSWORD: "123456"
          POSTGRES_HOST: postgres
          #ports: 5432:5432
    steps:
      - uses: actions/cache@v2
        with:
          path: chemotion_ELN/shared/bundle
          key: ${{ runner.os }}-gems-${{ hashFiles('**/Gemfile.lock') }}
          restore-keys: |
            ${{ runner.os }}-gems-
      # - name: testjs
      #   run: |
      #     cd chemotion_ELN
      #     pwd
      #     cp chemotion_ELN/shared/node_modules chemotion_ELN/shared/node_modules
      #     #RAILS_ENV=test bundle exec rake db:test:prepare --trace
      #     npm test
      - name: testrb
        run: |
          cd chemotion_ELN
          #gem install bundler -v 1.17.3 --no-document && 
          bundle install -j 3 --path shared/bundle
          #echo "OPENBABEL"
          #find -name openbabel.so
          #gem which openbabel
          rspec --exclude-pattern spec/{features}/**/*_spec.rb
          #source ~/.rvm/scripts/rvm && RAILS_ENV=test bundle exec rake db:test:prepare && bundle exec bundle exec rspec --exclude-pattern spec/{features}/**/*_spec.rb
      # - name: Setup tmate session
      #   uses: mxschmitt/action-tmate@v3
      # do new commit



